import React from 'react';
import { Container, } from 'semantic-ui-react';

const About = () => (
  <Container>
  <h1> About </h1> 
  <p> This is a simple Flashcard app written in React.js with React Router. The user is able to add, edit, and delete flashcards as well as show and hide the flashcard answer. Enjoy!</p>
  </Container>
);

export default About;